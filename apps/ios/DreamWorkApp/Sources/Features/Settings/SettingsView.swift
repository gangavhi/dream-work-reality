import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationStack {
            Text("Settings Screen")
                .font(.title2)
                .navigationTitle("Settings")
                .padding()
        }
    }
}
