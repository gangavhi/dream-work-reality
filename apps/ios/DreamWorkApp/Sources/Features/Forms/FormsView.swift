import SwiftUI

struct FormsView: View {
    var body: some View {
        NavigationStack {
            Text("Forms Screen")
                .font(.title2)
                .navigationTitle("Forms")
                .padding()
        }
    }
}
