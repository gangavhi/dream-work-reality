# Dreamwork Core (Rust Shared Core)

This directory contains the initial shared Rust core scaffold aligned with the ADRs.

## Prerequisites

- Stable Rust toolchain (via `rustup`)

## Build

```bash
cd core
cargo build --workspace
```

## Test

```bash
cd core
cargo test --workspace
```
